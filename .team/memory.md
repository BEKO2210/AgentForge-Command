# Team memory — durable decisions & learnings (LEAD-curated, append-only)

Read this at kickoff. Capture what should survive across runs: architecture
decisions, project conventions, recurring blockers and how they were fixed.
This persists context that the logs (per-run) and the board (current state) do not.

Format: `YYYY-MM-DD · <topic> — <decision / learning>`  (newest at bottom)

<!-- entries below -->
2026-05-26 · test-site — Demo-Webseite unter `test-site/` mit klarer Lane-Trennung: frontend=`public/`+`styles/`, backend=`server/` (Node http, keine Deps), quality=`tests/`. Lead pflegt nur README + integriert.
